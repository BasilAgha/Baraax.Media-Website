# Media

Place the final fullscreen hero video at:

`public/media/baraax-hero.mp4`

The site already includes a poster fallback at `public/media/hero-poster.svg`, so it looks complete while the real showreel is pending.
